import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#b86edd',
      main: '#9932cc',
      dark: '#732bbd'
    },
    secondary: {
      light: '#98dc76',
      main: '#65cc32',
      dark: '#3ea71f'
    },
  },
});