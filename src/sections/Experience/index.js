import * as React from 'react';
import { styled } from '@mui/system';

export default function Experience() {
    const ExperienceSection = styled('section')({

    })
    return <ExperienceSection id="experiences">Experience</ExperienceSection>
}